[options]
; Integration-test configuration shared by every Odoo container.
admin_passwd = it-master-password
list_db = True
; Subdomain routing like the production cloud: it15.localhost → database "it15".
dbfilter = ^%d$
data_dir = /var/lib/odoo
limit_time_real = 900
limit_time_cpu = 900
